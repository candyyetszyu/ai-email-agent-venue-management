{
  "name": "email-agent-backend",
  "version": "1.0.0",
  "description": "Backend for Email Agent application",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "dev": "node worker.js",
    "build": "npm install && echo 'Backend build completed'",
    "test": "jest"
  },
  "dependencies": {
    "googleapis": "^118.0.0",
    "hono": "^3.12.0",
    "jsonwebtoken": "^9.0.0",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}