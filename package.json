{
  "name": "ai-email-agent-venue-management",
  "version": "1.0.0",
  "description": "AI-powered email agent for venue management with bilingual support",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm run build",
    "start": "concurrently \"npm run start:backend\" \"npm run serve:frontend\"",
    "start:backend": "cd backend && npm start",
    "serve:frontend": "cd frontend && npx serve -s build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "deploy": "npm run build"
  },
  "keywords": [
    "ai",
    "email",
    "venue-management",
    "react",
    "nodejs",
    "cloudflare",
    "bilingual"
  ],
  "author": "AI Email Agent Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}